[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-infra-tools"
version = "0.1.0"
description = "ops-core central control plane for personal business services."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "httpx>=0.27",
  "pyyaml>=6.0",
  "typer>=0.12",
  "uvicorn>=0.29",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[project.scripts]
fastapi-message = "fastapi_message.cli:app"

[tool.setuptools.packages.find]
include = ["fastapi_message*", "ops_core*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
