service:
  name: ops-core
  host: 127.0.0.1
  port: 8080
  health_path: /health

paths:
  data_dir: data
  log_dir: logs
  log_file: logs/ops-core.log

security:
  require_admin_user: false
  max_output_chars: 4000

deploy:
  install_path: /opt/personal-infra-tools
  systemd_service: ops-core
